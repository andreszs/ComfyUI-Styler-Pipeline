# Changelog

## 1.0.5

- Fixed a 1.0.4 regression where AI presets/local style index initialization could fail with `byCategory[category] is undefined` when styles introduced categories outside the built-in category list.
